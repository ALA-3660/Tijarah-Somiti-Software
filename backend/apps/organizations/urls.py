"""
URL Routing for TSS Organizations Application (Foundation).
Future endpoints: /api/v1/organizations/, /api/v1/organizations/switch/
"""
from django.urls import path

app_name = 'organizations'

urlpatterns = [
    # Future Phase Foundation routes will be registered here
]
