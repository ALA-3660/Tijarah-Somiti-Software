"""
URL Routing for TSS Authentication Application (Foundation).
Future endpoints: /api/v1/auth/login/, /api/v1/auth/refresh/, /api/v1/auth/logout/
"""
from django.urls import path

app_name = 'authentication'

urlpatterns = [
    # Future Phase Foundation routes will be registered here
]
